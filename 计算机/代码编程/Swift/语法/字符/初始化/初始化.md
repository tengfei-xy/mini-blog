# 初始化

作为字符变量赋值，只有能有一个字符

```swift
let exclamationMark: Character = "!"
```

字符数组

```纯文本
let catCharacters: [Character] = ["C", "a", "t", "!", "🐱"]
```

字符数组转字符串

```swift
let catString = String(catCharacters)
```
