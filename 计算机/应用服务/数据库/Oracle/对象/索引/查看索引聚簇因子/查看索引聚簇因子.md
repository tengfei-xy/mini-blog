# 查看索引聚簇因子

完整版

```纯文本
SELECT OWNER,INDEX_NAME,INDEX_TYPE, TABLE_NAME,CLUSTERING_FACTOR,NUM_ROWS,DISTINCT_KEYS,BLEVEL,STATUS,LAST_ANALYZED,DEGREE,PARTITIONED FROM DBA_INDEXES WHERE TABLE_NAME = 'WFPROCESSDEFINE';
```

简略版

```纯文本
SELECT CLUSTERING_FACTOR FROM DBA_INDEXES WHERE STATUS = 'VALID' and TABLE_NAME = 'WFPROCESSDEFINE' and INDEX_NAME='PK_WFPROCESSDEFINE';
```
